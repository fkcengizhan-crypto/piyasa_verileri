name: Piyasa Verileri Çek

on:
  schedule:
    - cron: '30 7-15 * * 1-6'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  veri-cek:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      # ... diğer adımlar aynen kalır ...
